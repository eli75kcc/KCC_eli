import React from "react";
import ReactDOM from "react-dom/client";
import VoiceChatBot from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VoiceChatBot />
  </React.StrictMode>
);
